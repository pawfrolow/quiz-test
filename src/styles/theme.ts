import { DefaulTheme } from 'styled-components';

export const theme: DefaulTheme = {
    sizes: {
        navbarHeight: 76,
    },
};

export const antTheme = {
    token: {
        colorPrimary: 'rgba(185, 22, 180, 1)',
    },
};
